import { test } from '@japa/runner'

test.group('Usuario store', () => {
  test('armazenar um usuário com sucesso')

  test('exigir parâmetros obrigatórios ao armazenar um usuário')

  test('validar email do usuário')

  test('validar telefone do usuário')

  test('proibir nome do usuário maior do que 25 caracteres')
})
