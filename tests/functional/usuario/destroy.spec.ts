import { test } from '@japa/runner'

test.group('Usuario destroy', () => {
  test('remover um usuário com sucesso')

  test('falhar caso um usuário não for encontrado')

  test('remover os dados de pets ao remover um usuário')
})
